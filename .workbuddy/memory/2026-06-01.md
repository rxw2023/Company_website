## 2026-06-01

### 落地页集成到 React 项目（已废弃，见下方合并）
- 将 `landing.html` 转换为 React 组件 `src/pages/LandingPage.tsx`
- 添加 `/landing` 路由到 App.tsx
- 删除旧的独立 `landing.html`

### Home.tsx 与 LandingPage.tsx 合并
- 将两者合并为单一 Home.tsx（Apple 风格 + 原全部数据）
- emoji 图标 → 内联 SVG 图标（麦克风、摄像机、音箱、地球）
- 保留 SeoHead、全部 11 款产品（含 a11 占位）、全部 12 个案例
- 删除 LandingPage.tsx，移除 /landing 路由
- 首页 `/` 即为融合后的 Apple 落地页风格
- TypeScript 编译通过，零错误

### Home.tsx 样式优化（浅色主题 + 交互修复）
- 整体风格从黑底 → 白底 Apple 浅色主题（Hero/Features/Cases/Footer 全部变白）
- 思必驰 AISPEECH logo 加入 Hero 区域顶部
- 公安备案盾牌图标加入 Footer 备案号旁
- 所有联系 CTA 按钮（导航"咨询热线"、Hero "样品/预约体验"、关于"预约体验/申请样品"、CTA "联系我们"）→ 改为 scrollTo('footer')，点击滚动到页脚联系方式
- Footer 新增 id="footer" 作为滚动目标锚点

### 细节修复
- 公安图标 → 官方图片 `beian.mps.gov.cn/img/logo01.dd7ff50e.png`
- 思必驰 Logo 高度 32px → 48px
- Hero 小标题 "授权代理商" → "思必驰 AISPEECH"；产品区 "产品系列"；案例区 "案例集锦"
- 产品描述文字 max-width 解除限制，一行显示
- "深耕 B2B 集成" → "企业级系统集成"
- Logo 颜色冲突修复：包裹白色圆角卡片 + box-shadow，高度 192px → 48px
- 导航 "恒迪视讯" 从 Link to="/" → onClick scrollTo('about')
- Git remote HTTPS → SSH，已推送 commit e3b3848
- favicon 替换为 HD.jpg（`public/favicon.webp`）
- 导航栏「恒迪视讯」左侧添加 HD logo 图标（28px 高）

### ProductDetailPage 优化
- 进入产品详情页时 `window.scrollTo(0, 0)` 确保从顶部开始显示
- 整体改为灰色调：主背景 `bg-gray-50`、规格表 `bg-gray-100`/`bg-gray-50` 交替、FAQ 区 `bg-gray-200/60`
- 描述文字改为 `text-gray-500`、分类标题 `text-gray-500`、表格内容 `text-gray-600/700`
- 移除所有 `dark:` 相关的 Tailwind 类（统一浅色主题）
