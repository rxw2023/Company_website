## 2026-06-02

### a2产品页 - 相关产品卡片改造
- 将 `product/a2` 页面的"可选控制器"链接替换为"相关产品"卡片
- 展示 AIMIC-B100 (a10) 桌面控制器的产品卡片（图片+型号+描述）
- 卡片格式：白底圆角边框，hover蓝色描边+阴影，顶部图片，下方型号/名称/描述
- 点击卡片跳转到 `/product/a10`
- 构建通过，sitemap自动生成25个URL
- 改为数组驱动 + `grid grid-cols-2` 两列布局，新增 a9（C60摄像头）作示例
- 改为水平滚动轮播（4个产品：a10/a9/a3/a6），左右箭头按钮，useRef 控制滚动
- 箭头根据滚动位置自动显隐（`canScrollLeft`/`canScrollRight`），卡片固定260px宽度
- 修复点击跳转失效：hooks 在 IIFE 内违法调用 → 提取为独立 `RelatedProducts` 组件，符合 React Hooks 规则
- 新增 a1 页面相关产品（MA600D + AIMIC-B100 + C60）
- 全局图片容器底色统一：`bg-gray-100` / `#fafafa` → `bg-white` / `#fff`（ProductDetailPage 两处 + Home.tsx `.hd-product-img-wrap`）
- aispeech-logo.png 图片背景非纯白 → Python Pillow 处理，近白色素转为 #ffffff，保持透明度
- 产品图片统一改为案例格式（`object-fit: cover`）：Home.tsx .hd-product-img-wrap + ProductDetailPage 相关产品卡片 + 产品大图 + 缩略图，去掉背景色和 padding，hover 放大效果
- 已回退上条（理解有误）；实际需求：产品详情页背景 `bg-gray-50` → `bg-white`，与案例页 `.hd-section-light` 白底一致
- 新增 `Navigation.tsx` 共享导航组件（首页风格：固定顶部、毛玻璃背景、Logo+产品/案例/关于我们+咨询热线）
- 导航加到 ProductDetailPage、CaseDetailPage、NotFoundPage，非首页点击跳转首页对应锚点
- Header.tsx 精简：去掉"杭州恒迪视讯技术有限公司"文字，仅保留 AISPEECH logo 并放大（220→360 / 320→480），点击 logo 返回首页
- FaqPage 加上 Navigation 导航栏
- 已提交并推送到 GitHub (rxw2023/Company_website, main 分支, commit 2504f9c)
- Navigation.tsx 新增「首页」导航链接（Logo后可点击首页，非首页跳转 `/`，首页滚动到顶部）
- 删除所有「返回」按钮：ProductDetailPage、CaseDetailPage、NotFoundPage（导航栏已有首页入口）
- Home.tsx 内联 `<nav>` 替换为共享 `<Navigation />` 组件，全站导航统一
