# 2026-05-27

- 首页添加 Canvas 像素网格动画背景（来源 `C:/Users/1122/WorkBuddy/2026-05-27-15-16-50/canvas-pixel-grid.html`），只保留背景效果，无文字无控件。canvas 为 fixed 定位 z-index:0，pointer-events:none，`#root` 为 relative z-index:1。
- 修复构建错误：`aispeech-logo.webp` 在 dist 存在但 src 缺失，从 dist 复制到 `src/assets/images/`。
- 构建验证通过。
- 修复 Canvas 背景被覆盖：Home/CaseDetail/ProductDetail 三个页面最外层 div 有 `bg-white` 类，React 渲染后盖住 canvas。去掉 `bg-white`，让 canvas 背景正常显示。
