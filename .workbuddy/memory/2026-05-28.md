# 2026-05-28 工作日志

## GEO 优化（全部完成，构建通过，commit 02ef355，push 因网络问题待重试）

1. **结构化数据 Schema（Task 1）**
   - `index.html` head 加入 Organization + WebSite JSON-LD Schema
   - 新建 `src/components/SeoHead.tsx` 组件（react-helmet-async），支持 Product/BreadcrumbList Schema 动态注入
   - `src/main.tsx` 包裹 `<HelmetProvider>`
   - 首页、产品详情页、案例详情页均引入 SeoHead，每页有唯一 title/description/canonical

2. **FAQ 页面（Task 2）**
   - 新建 `src/pages/FaqPage.tsx`，包含 13 条问答（公司服务/产品特点/安装技术/应用场景）
   - 自带 FAQPage Schema（JSON-LD），帮助 AI 搜索引擎引用
   - 在首页底部加 "常见问题 FAQ →" 入口链接

3. **meta description 每页唯一（Task 3）** — 随 SeoHead 一同完成

4. **语义化 HTML（Task 4）**
   - 首页产品区用 `<section>` / `<article>` / `<footer>` / `<main>` 替代纯 `<div>`
   - 图片 alt 属性补充为具体描述（含产品名+品牌+场景）

5. **robots.txt + sitemap.xml（Task 5）**
   - `public/robots.txt`：明确放行 GPTBot/ChatGPT-User/PerplexityBot/Claude-Web/anthropic-ai
   - `public/sitemap.xml`：覆盖首页、FAQ、10个产品页、12个案例页，共 24 个 URL

## 依赖变更
- 新增 `react-helmet-async` 用于动态注入 meta 标签和 JSON-LD Schema
